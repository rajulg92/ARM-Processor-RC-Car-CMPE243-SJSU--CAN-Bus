//#include "foo.hpp"
//#include "turtle.hpp"
#include "sensorVal.hpp"
#include "getPwmmotor.hpp"

#if 0
int SensorVal::readSensorValue(int sensorValue)
{
   if(sensorValue < 0 || sensorValue > 100)
      return -1;
   if(senorValue >= 0 && senorValue < 50)
      return 8;
   if(senorValue >= 50 && senorValue <= 100)
      return 9;
}
#endif
